{
  "name": "logger",
  "version": "1.0.0",
  "description": "Logger for NodeJS application stack",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "NODE_ENV=test istanbul cover node_modules/mocha/bin/_mocha --root ./lib --include-all-sources --root ./ -- --recursive",
    "mocha": "NODE_ENV=test node_modules/mocha/bin/_mocha test/ --recursive"
  },
  "keywords": [
    "logger"
  ],
  "author": "Chauffeur-Privé",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "co-mocha": "^1.1.2",
    "eslint-config-cp": "github:transcovo/eslint-config-cp#1.1.0",
    "istanbul": "^0.4.2",
    "mocha": "^2.4.5",
    "mocha-lcov-reporter": "^1.0.0",
    "nock": "^7.0.2"
  }
}
